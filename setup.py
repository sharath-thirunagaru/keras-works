from setuptools import setup,find_packages

setup(name='keras works',
      version='0.1.0',
      author='sharath',
      author_email='sharath.tds@gmail.com',
      packages=find_packages(exclude=[])
    )